---
kind: record
status: recorded
audience: maintainer
source_of_truth: self
updated: 2026-09-14
---

# 开放插件生态历史里程碑

本文保存早期 M1–M6 讨论和已发生的交付证据。当前行为见[总计划](../../plans/runtime-v2/open-plugin-ecosystem.md)和相关 Spec；历史测试结果不代表本轮验证，也不定义未来执行顺序。

| 提交 | 当时的工作 |
|---|---|
| `9b34692`、`cd4e6d3`、`878eb76`、`abc5420` | 讨论开放插件、薄宿主、未来 Agent 边界及迁移验收要求 |
| `d211892` | Context 初版区分规则与资料，随后被 M1 调整 |
| `2097b73` | M1 删除 Host 用途分类，保留原始贡献；默认消费者拥有兼容裁剪与预算 |
| `f9fde091` | M2 引入执行器选择、专注样例、进度、取消与跨进程结果提交 |
| `99039af` | 增加已有数据启动器，复用角色、模型和配置 |
| `dd5f5e8` | 撤回主设置“互动方式”和正常聊天执行器选择，仍保留开发实验 |
| `b641200` | 公共 `context.bind()` 固定进程实例，TTS Hub 隔离重载前后同名任务 |
| `7751295` | 便签记忆以独立存储、Collection、Context 和工具验证第二记忆实现 |
| `4bd53bc5` | 修复便签显式搜索漏查已设关键词记录的正文 |
| `90c2e4fb` | ASR Hub/Provider 固定实例，隔离查询、取消及首次交付前的迟到结果 |
| `dd12eaf7` | 统一 Collection 转场保护，修复重叠通知及同角色重启丢草稿 |

M2 当时通过真实 Core/插件进程的受理、取消、重载与唯一提交回归，包含 Rust/JavaScript 桥和开发构建；
Edge 设置页使用模拟原生 IPC 联接，空白环境验证了无模型计时和立绘加载。这些不是原生桌面、设备与真实模型的完整人工验收。
选择入口后来撤回，2026-09-14 审查收尾进一步删除无产品消费者的执行器实验及专注样例。旧代码和旧计划可以在上述 Git 提交中查看。

已有记录中的聚焦自动结果：TTS/服务绑定 91 项；Mem0/便签初版 45 项；便签搜索与升级保留 15 项；
ASR journey 为 Python 45 项、Rust 11 项、前端 22 项，真实麦克风用例未执行；`dd12eaf7` 的相关前端 96 项通过。
这些数字只对应当时测试范围。用户便签试用另经本地 Trace 确认在正常 Assistant 生效；仓库不复制其私密内容。

M3 的模型提供者迁移、M4 的默认对话策略迁移当时均未完成；M5 的实例绑定与既有插件验证、M6 的设置容器改造不能替代这两项核心工作。
本次收尾的架构取舍见 [ADR-0051](../../adr/0051-retire-executor-experiment-and-scope-collections.md)。
