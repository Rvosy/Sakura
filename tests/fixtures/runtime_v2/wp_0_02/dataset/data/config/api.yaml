llm:
  base_url: https://fixture.invalid/v1
  api_key: REDACTED_FIXTURE_VALUE
  model: fixture-model
  timeout_seconds: 60
api_profiles:
  - id: fixture
    alias: Fixture Provider
    base_url: https://fixture.invalid/v1
    api_key: REDACTED_FIXTURE_VALUE
    fixture_provider_unknown: preserve-provider
    models:
      - name: fixture-model
        fixture_model_unknown: preserve-model
model_slots:
  chat:
    profile_id: fixture
    model: fixture-model
tts:
  enabled: false
  provider: none
fixture_unknown:
  nested: preserve-top-level
