# 更新日志

## 0.9.9-dev - 开发中

这一节记录 `dev` 分支已经合入、准备进入 0.9.9 的改动。开发版仍可能调整配置和界面，不建议拿它替代日常使用的稳定包。

### 设置与首次启动

- 设置页改用 Tauri 界面，模型、角色、TTS、插件和系统选项集中在同一个窗口中。
- API 配置现在可以保存多套供应商，并分别给聊天、视觉和记忆整理选择模型。没有单独指定时，视觉和记忆任务会沿用聊天模型。
- 首次启动会引导选择或导入角色，再完成供应商和模型配置。已有配置不会重复进入引导。
- 主动截图可以选择分辨率。多显示器混合缩放下的截图坐标和尺寸也做了修正。
- Runtime v2 固定桌宠界面已接入真实聊天：支持发送、取消、错误恢复、连续对话和完整回复逐字展示；
  生成期间发送图标会变为可点击取消的环形旋转条，Core 重启或旧回复晚到时不会覆盖当前界面。
- Runtime v2 恢复旧版聊天表现：启动问候在窗口显示后逐字播放，多段回复按段清屏并同步立绘；等待回复
  保持当前立绘；右键菜单可持久化切换中文翻译/日文原文；立绘交叉淡入不再因快速切换闪白或回跳。
- Runtime v2 等待回复时恢复旧版点号动效，输入框会提示当前角色正在思考；移除“立即显示”按钮，并让
  HTTP 400/429/5xx 错误显示状态码和经过脱敏的供应商诊断，不再只显示泛化英文。
- Runtime v2 中日字幕切换会立即刷新当前气泡；气泡右侧恢复当前运行会话内的回复上下翻阅，并同步切换
  对应立绘。气泡右上角关闭按钮已移除，鼠标右键菜单也不会再把“隐藏至托盘”持续显示为选中高亮。
- Runtime v2 回复轨道不再压缩原气泡高度；气泡与输入栏的自适应高度改为原生确认后整块提交，修复输入
  扩展时发送按钮先下沉、被裁切以及 DOM 几何尾帧造成的撕裂感。
- Runtime v2 桌宠原生窗口改为随立绘可见 alpha、气泡、输入框和菜单动态收紧；Windows、macOS 与
  Linux 均应用精确输入区域，使立绘外围和内部透明洞可点击穿透。表情、缩放和控件高度变化保持立绘
  底部中心不跳动，可见立绘顶部也能贴近屏幕上沿；只有立绘有效像素可以启动拖动。
- 修复 macOS Runtime v2 拖动立绘时 WKWebView 把整张透明图片显示为蓝色选区或拖拽预览的问题；
  立绘不再可选，气泡正文和输入框文字仍可选择，选区高亮统一跟随角色主题。
- 修复 Runtime v2 连续缩放立绘时动态原生窗口与 WebView 异步补偿导致气泡、输入框抖动的问题；
  同一立绘缩放改用最大倍率稳定包络，缩放手势由按下/松开显式控制，修复 50%–55% 慢速微调时计时
  收口造成的向上闪动。Windows 在取得立绘 alpha 后保持稳定的底层窗口/WebView 包络，只用精确 region
  表达静止态真实可见范围和点击穿透，避免手势两端 resize/reposition 产生瞬时跳位。
- 修复 Windows 缩放立绘时逐刻度重建复杂 `SetWindowRgn` 导致画面迟滞、旧遮罩裁剪新帧的问题；
  拖动刻度通过独立轻量事件直接更新 WebView 合成 transform，不再等待完整外观预览、窗口 bounds 或
  alpha 模型；相邻快速点拖共享同一原生 guard，最后松手只恢复一次最终倍率的逐像素 region。轻量帧
  短暂丢失会内部追赶最新值，角色代际短暂无读值则有界重试，不再弹出内部连接错误或旧倍率回跳。
- Runtime v2 的对话框高度改为完全由设置值控制，回复长度和逐字输出只在框内滚动，不再改变外框；
  宽度、高度、整体上下位置和输入栏位置在 Windows 上也改用稳定包络内的轻量实时帧，松手才提交一次
  原生窗口和精确遮罩。拖动开始不再等待遮罩准备；立绘预先建立合成层，改善首次拖动缩放或布局
  滑块时无响应、卡顿和需要重复拖动的问题。
- 修复 Windows Runtime v2 首次提交动态窗口位置时同步移动回调重入几何锁，导致进程存活但桌宠窗口
  始终隐藏的问题；程序化移动不再阻塞窗口事件循环，真实拖动锚点记录保持不变。
- Runtime v2 设置页新增“字幕逐字间隔”和“回复分段停顿”，保存后从下一条回复起生效；固定窗口不兼容
  的气泡自动隐藏和自由布局选项继续保持禁用。
- Runtime v2 设置页开放长期记忆管理和整理频率；记忆整理模型槽与固定 embedding 模型安装/ZIP 导入
  统一放在“模型”页，“记忆”页恢复紧凑统计、筛选和列表/编辑器双栏。模型任务显示真实阶段与进度并可
  取消，下载/导入失败保留旧缓存；记忆检索、模型或存储故障会降级为空召回，普通聊天继续完成且不会
  自动重发。
- 修复记忆模型设置触发 Core 重启后设置窗口继续使用旧 generation 的问题；窗口现在原位重绑定并保留
  列表、筛选、选中项、草稿与中文/日文输入法组合文本，不再要求关闭重开，也不会让旧代错误覆盖页面。
- 修复本地记忆模型首次加载 PyTorch 时阻塞 Python Core、引发连续 generation 重启和
  `SETTINGS_CORE_GENERATION_MISMATCH` 的问题；固定 embedding 推理改由 Core 管理的隔离子进程执行，
  加载期间聊天与设置保持响应。设置页会在两分钟有界预算内保持“正在初始化”并自动重试只读搜索，
  不清空列表或草稿；完成后自动显示记忆，加载中退出也会有界清场。
- Runtime v2 只在 Core 协商 `assistant.memory` 后打开 Memory 存储；手工新增、编辑、幂等删除与完成回复
  后的自动整理均绑定当前角色和 Core generation，Core 恢复期间保留设置页中的中文/日文 IME 草稿。
- Runtime v2 的 Python Core 意外退出后会在原桌宠窗口内自动恢复：旧代请求和资源立即失效，活动回复
  进入明确的中断终态，已完成回复、回复翻阅和未发送草稿继续保留；新状态与立绘重新绑定完成前不会开放
  发送或自动重发，连续崩溃耗尽预算后可从发送按钮位置手动重试。
- Runtime v2 在创建可写聊天会话前会校验当前数据版本和聊天历史；未来版本、损坏或越界的历史会进入稳定
  只读失败状态，不调用模型、不追加、截断或自动修复原文件，避免破坏迁移前的数据基线。
- Legacy Qt 不再作为面向用户的兼容回退；迁移期间只保留为开发对照和数据 oracle，Runtime v2 完成全部
  能力迁移并通过发布总门后将删除 Qt 桌宠入口与实现。

### 手机网页端

- 新增可选的 `sakura_mobile` 插件。手机浏览器可以查看当前角色的聊天记录，发送文字或图片，并把回复同步回桌面端。
- 手机端复用桌面的模型、记忆和当前角色，不会另起一套会话。为避免远程误操作，它不开放宿主工具，也不能在网页里切换角色。
- 网页和接口使用访问 token。局域网使用时不要把端口映射到公网；需要远程访问时，建议配合 Tailscale。

### 更新与发布

- Windows 包新增 `update.bat`。它会检查更新清单和 SHA-256，覆盖程序文件时保留 `data/`、角色、运行环境与用户插件配置。
- 发布包进一步缩小体积，工作流补上更新包、删除清单和校验文件的生成步骤。
- 角色工作室统一为 Sakura 内打开的 Tauri 界面；升级时会移除 `start_studio.bat` 和旧 PySide6 工作室文件。
- 整理源码仓库根目录：文档素材归入 `docs/`，开发依赖归入 `tools/`，升级删除清单改为仅在构建更新包时注入。
- Intel macOS 的 NumPy/Transformers 兼容约束已并入 `requirements.txt`，无需再安装单独的约束文件。

### 稳定性修复

- 统一运行日志，减少控制台重复输出，并补齐模型请求、工具调用和后台任务的关键信息。
- 修复手机端忙碌状态、聊天回溯、记忆复用和模型切换后的配置刷新问题。
- 加固角色包导入导出、历史记录读取、存储迁移、TTS 播放和后台任务退出流程。
- Playwright 浏览器、MCP、工具参数校验和网络请求增加了更严格的边界检查。

## 0.9.8 - 2026-06-21

### 本地快速接话

等完整回复的时候，Sakura 不用再沉默了。她会先接一句符合当前语气的短回应——比如你说了一长串，她先回个"嗯，我在听"之类的。可以在设置里调延迟、概率和语音，不喜欢直接关掉也行。

### 长期记忆升级

之前记忆是一锅粥，现在分成了几层：核心信息、经历、任务、操作习惯，各自归类。聊天的时候 Sakura 会自动翻出相关内容，也会在对话中悄悄更新记忆。重复和冲突的信息她会自己整理。

### 插件系统

插件系统重写了。现在插件可以注册工具、设置页、事件和渲染器，插件自己的设置也移到了独立窗口。管理页上能看到每个插件的版本、权限和状态。Playwright 浏览器插件继续带着。

> 旧版插件如果用的是 `sdk.*` 接口，升级后可能用不了，需要插件作者按新规范适配。

### 角色工坊

Windows 用户双击 `start_studio.bat` 就能打开。这是个图形界面的角色编辑器：新建角色、写人格卡、搭主题配色、拖立绘和参考音频进去，最后导出 `.char` 文件，别人也能导入用。

### 其他

- 截图在高 DPI、多显示器和 Retina 下的错位和模糊修了。
- 点击桌宠就能叫出输入栏，不用每次都去找按钮。设置、历史和日志窗口现在有自己的任务栏图标，也不会被置顶的桌宠压住。
- 意外关掉重开，Sakura 会记得上一段聊了什么，不至于跟失忆一样。
- TTS 重做了底层，切换设置、退出程序和本地语音断开不会再崩。
- `0.9.8` 发布包不包含 `data/`，直接覆盖升级不会吃掉你的配置和聊天记录。
