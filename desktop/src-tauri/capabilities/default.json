{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default-deny capability for the pet and settings window hosts.",
  "windows": ["main", "settings"],
  "permissions": []
}
