{
  "name": "sakura-telemetry-edge",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply sakura-telemetry --local",
    "db:migrate:remote": "wrangler d1 migrations apply sakura-telemetry --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "latest",
    "wrangler": "latest"
  }
}
